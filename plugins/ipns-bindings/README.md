# IPNS Plugin

Wrap ipns-entry in a libp2peasy plugin.

## Emit IPNS Record

Publishes an IPNS record to the network.

## Handle IPNS Records

Processes a receives message from the network, determines if it is an IPNS record, and if so, emits an event.
